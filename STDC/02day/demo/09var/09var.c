#include<stdio.h>
int main () {

int val=1,val1=10;  //声明整型变量val与val1，并加以初始化。
printf("%d*%d=%d\n", val,val1-val,val*(val1-val));

val=val+1;  //根据val变量中原有的内容＋1，得到下一个数字，并记录到val变量里。
printf("%d*%d=%d\n", val,val1-val,val*(val1-val));

val=val+1;  //根据val变量中原有的内容＋1，得到下一个数字，并记录到val变量里。
printf("%d*%d=%d\n", val,val1-val,val*(val1-val));

val=val+1;  //根据val变量中原有的内容＋1，得到下一个数字，并记录到val变量里。
printf("%d*%d=%d\n", val,val1-val,val*(val1-val));

val=val+1;  //根据val变量中原有的内容＋1，得到下一个数字，并记录到val变量里。
printf("%d*%d=%d\n", val,val1-val,val*(val1-val));

val=val+1;  //根据val变量中原有的内容＋1，得到下一个数字，并记录到val变量里。
printf("%d*%d=%d\n", val,val1-val,val*(val1-val));

return 0;
}
